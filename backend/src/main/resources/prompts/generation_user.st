섹션 {no} '{title}' (p.{pageStart}-{pageEnd})의 기획서 내용이다. 먼저 이 섹션의 기능/UI/아트 요소를 목록화(elements)하고, RULES에 따라 test_cases와 questions를 JSON으로 생성하라. test_cases에는 No를 포함하지 마라 (번호는 별도로 부여한다). elements의 모든 항목이 test_cases 또는 questions 중 한쪽에 반드시 반영되어야 한다. questions의 text 필드에는 질문 내용만 쓰고 출처(페이지/슬라이드 번호)는 절대 포함하지 마라 — 출처는 반드시 별도의 source 필드에만 적어라 (예: text='군량미 최대치는?', source='p.5').

{context}

## 유사 참고 TC (RAG 검색 결과)
{referenceExamples}
